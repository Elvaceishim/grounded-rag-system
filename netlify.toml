# Netlify configuration for frontend deployment
# This is a STATIC SITE - no Python needed!

[build]
  # Just publish the static folder
  publish = "static"
  # Simple echo command - no real build needed
  command = "echo 'Deploying static site'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
